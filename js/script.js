// Feahter Icon 
feather.replace();

// Banner Slider
$('.ban_slide_item').slick({
    dots: true,
    infinite: false,
    speed: 300,
    slidesToShow: 1,
    adaptiveHeight: true,
    arrows:false,
    infinite:true,
    autoplay:true,
  });

//Video
new VenoBox({
  selector: '.appon_video',
  maxWidth: "70%",
}); 

// Feedback Slider


